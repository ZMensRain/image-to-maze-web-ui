{
  "name": "image-to-maze",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm build-go && tsc && vite build",
    "preview": "vite preview",
    "build-go": "cd src/lib/go/ && make && cd ../../../"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
